[package]
name = "v2-ffi-cpp"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "第 7 课：为一个 C 库构建对外完全安全的 Rust 封装（cc 编译 C + 手写 extern 声明 + CString/CStr 跨界）"
build = "build.rs"

[dependencies]
libc = "0.2"

[build-dependencies]
cc = "1"
